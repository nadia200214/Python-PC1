# Definir la lista original
lista_original = ['Obedece', 'a', 'tu', 'mamá']

# Crear una nueva lista que sea la inversa de la lista original
lista_invertida = lista_original[::-1]

# Imprimir la lista invertida
print(lista_invertida)